/* Take your solution from exer 11-1 and split the module into several files: a primary module interface file without
   any implementations and two module implementation files, one for the CarSimulator and one for the BykeSimulation
   class. */

import simulator;

int main()
{
	using namespace Simulator;

	CarSimulator carSim;
	BikeSimulator bikeSim;
}